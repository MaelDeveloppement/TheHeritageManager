[build]
builder = "nixpacks"
buildCommand = "npm run build --workspace=apps/web"

[deploy]
startCommand = "npx vite preview --config apps/web/vite.config.ts --outDir apps/web/dist --port 3000 --host 0.0.0.0"